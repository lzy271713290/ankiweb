# DESIGN.md

## 气质与意象

Anki 卡片生成器——知识从"阅读"到"记忆"的桥梁。意象锚点：清晨书房里一摞整理有序的索引卡片，桌面干净，光从左侧窗户照入。专业但不冰冷，沉静且有秩序感。

## 配色方案

- **主色**：靛蓝 `oklch(0.54 0.25 265)` — 学术、专注、深沉
- **强调色**：琥珀金 `oklch(0.75 0.18 75)` — 用于 cloze 填空标记、活跃状态
- **背景**：纯净白 / 深灰黑（暗色模式）
- **卡片表面**：略带温感的浅灰底，模拟纸质卡片质感

## 字体排版

- UI 字体：系统默认无衬线（PingFang SC / Inter）
- 等宽字体：用于代码片段展示
- 卡片标题 18px bold，正文 15px，元信息 12px muted

## 视觉策略

- 卡片式布局，每张知识卡片模拟实体翻转卡片
- 流式生成时卡片逐张浮现（fade-in + slide-up）
- 卡片翻转预览（正面问题，背面答案）
- 整体留白充足，减少视觉噪音

## 页面结构

- 顶部：紧凑导航栏 + 标题 + 导出按钮
- 左栏（40%）：文本输入区 + 生成选项
- 右栏（60%）：卡片预览列表 + 统计面板
- 响应式：移动端上下堆叠

## 动效与交互

- 卡片入场：`opacity 0 → 1` + `translateY(12px) → 0`，300ms ease-out
- 卡片翻转：`rotateY(180deg)` transform，600ms
- 悬停：卡片轻微上浮 `translateY(-2px)` + 阴影加深
- 流式状态：脉冲式 loading 指示器

## 设计禁忌

- 不要使用蓝紫色渐变作为主视觉
- 不要过度装饰，保持工具属性
- 不要用过多 emoji 图标
