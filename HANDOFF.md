# AnkiCard AI 跨设备交接

> 最后更新：2026-08-02（家里电脑）
>
> GitHub：<https://github.com/lzy271713290/ankiweb>
>
> 当前分支：`main`
> 本文件只记录项目状态、决策和待办。禁止写入 API Key、Webhook、密码或其他凭据。

## 1. 项目定位与当前商业方向

AnkiCard AI 用于把教材、论文、笔记、字幕和题库整理为可编辑的高质量记忆卡片，支持保存卡组、导出 Anki APKG，并向飞书或企业微信推送学习内容。

现阶段不做 Anki Desktop/AnkiDroid 的完整二次开发，优先验证两条路线：

1. **闲鱼售卖精品卡组**：先做软考中级“系统集成项目管理工程师”，用真实商品和低成本样品验证需求、定价、转化与售后问题。
2. **会员 SaaS**：继续增强“材料导入 → 科学拆卡 → 人工微调 → 保存/导出/推送”的完整流程，等卡组销售验证用户需求后再增加账号、云同步、额度和会员体系。

内容产品原则：以教材、大纲和公开题库的高频考点为依据；卡片表述自行整理，不照搬受版权保护的整本教材或题库解析；正式售卖前必须人工复核。

## 2. 技术栈

- Next.js 16（App Router）
- React 19
- TypeScript 5
- Tailwind CSS 4
- shadcn/ui
- pnpm（不要使用 npm 或 yarn）

## 3. 当前已经完成

### 3.1 主流程与界面

- 参考 `cardflow-demo.html` 改造了主要界面风格。
- 支持问答、填空、定义、双向、对比、步骤六种卡片。
- 支持生成后翻面预览，并逐张编辑问题、答案和分类。
- “我的卡组”使用服务端 SQLite `data/decks.sqlite` 保存，支持重新载入和删除；旧版浏览器 LocalStorage 卡组首次打开时会自动迁移。
- 支持导出 `.apkg`，双向卡会生成正反两个 Anki 模板。
- 支持 txt、md、csv、json、html 等纯文本文件导入和 URL 抓取。
- 已移除 Trae、Coze、扣子相关代码和引用。
- Windows 下 `pnpm dev` 已改为直接运行 Next.js，不再依赖 Bash。

### 3.2 模型能力

- 支持 DeepSeek V4 Flash、DeepSeek V4 Pro。
- 支持通过 OpenAI Chat Completions 兼容接口注册多个模型。
- “模型设置”页面支持服务端连通测试。
- API Key 只由服务端读取，不会下发到浏览器。
- `ENABLE_DEMO_MODEL=true` 时会显示“本地演示模型”，可在不消耗额度的情况下测试完整页面流程。

### 3.3 本次新增：智能分析与科学拆卡（页面版本 v0.4）

- 新增 `POST /api/analyze-content`：生成前先分析材料结构、原子知识点、核心知识点、公式、对比和流程信号。
- 分析结果提供三档数量：
  - 精简复习：核心与高频。
  - 标准推荐：覆盖度与复习负担平衡，默认选择。
  - 全面覆盖：包含更多细节和应用。
- 支持用户自定义数量，低于建议下限时提示可能漏点，高于上限时提示可能重复。
- 材料或模型改变后，旧分析自动失效，必须重新分析再生成；卡片类型和难度不影响知识点识别，可在分析后直接切换。
- AI 分析不可用时自动使用本地规则估算，不会阻断生成流程；页面会标明“AI 分析”或“本地估算”。
- 页面默认折叠四条科学原则，用户需要时可点击查看：
  - 一卡一个知识点。
  - 问题脱离原文仍能独立理解。
  - 答案保持最小必要信息，过长则拆分。
  - 忠于用户原文，不补写没有依据的事实。
- 生成提示词同步加入原子化、去重、来源章节、避免模糊指代和避免为凑数生成低价值卡片等约束。
- 单次分析/生成最多读取材料前 20,000 个字符；超过时会提示本次只分析前 20,000 字符。
- 单批最多生成 120 张卡片。

涉及的主要文件：

- `src/app/page.tsx`：分析按钮、分析结果、数量方案、自定义警告、科学原则和生成前校验。
- `src/app/api/analyze-content/route.ts`：AI 内容分析、本地降级估算、建议数量计算。
- `src/app/api/generate/route.ts`：科学拆卡提示词、数量上限、分析结果注入和来源章节。
- `src/lib/types.ts`：分析结果、知识点和生成方案类型。
- `src/lib/server/llm.ts`：本地演示模型同时支持分析和生成。

### 3.4 推送能力

- 支持飞书群机器人和企业微信群机器人即时推送。
- 服务端可识别 Webhook 是否配置，页面“推送学习”可选择已保存卡组发送。
- 当前是群机器人 Webhook，不是可按用户单聊的飞书应用机器人。

### 3.5 软考卡组样品

- 已建立 `content/decks/system-integration-pm-sample.json`。
- 当前包含 30 张“系统集成项目管理工程师”高频考点样卡，状态均为“已初审”。
- 已覆盖项目管理概论、五大过程组、范围、进度、成本、挣值、风险、变更、配置、采购和干系人等主题。
- 每张卡带有教程章节、来源链接、审核状态和备注，便于后续复核。
- `scripts/export-sample-deck.ts` 可从 JSON 生成 APKG 和 CSV。
- 已生成 `outputs/system-integration-pm/2026软考中级·系统集成项目管理工程师·高频考点样卡.apkg`。
- 公司电脑另有 CSV 和 XLSX 审核表，但 `.gitignore` 默认排除了 `*.csv`、`*.xlsx`，不会上传；JSON 是可继续维护的主数据。

### 3.6 本次新增：模型 Token 用量账本

- 所有内容分析、卡片生成和模型连通测试都在服务端统一记录用量。
- 记录模型、时间、用途、状态、耗时、输入/输出/总 Token、缓存命中/未命中、推理 Token 和非敏感请求参数。
- 明细已从本地 JSONL 迁移到 `data/llm-usage.sqlite`，当前随 Git 提交，用于公司与家里电脑顺序同步总数；不记录 Key、Webhook 或用户材料原文。
- 新增 `GET /api/usage`，模型设置页展示累计统计和最近明细。
- 新增本地管理员页 `/admin/usage`：展示今日、历史总计、每日汇总、按模型/用途汇总和最近 500 次请求明细，并支持按日期、用途、模型筛选。
- 每日统计默认使用 `Asia/Shanghai` 时区，可通过 `USAGE_TIME_ZONE` 修改。
- 当前尚无账号系统，该页面是本地管理员工具，不代表已经完成线上管理员权限隔离。
- 已用 DeepSeek V4 Flash 实测：连通测试、流式分析和流式生成均成功返回并落账。
- 临时同步约定：换电脑前先提交并推送 SQLite，另一台开工前先拉取；禁止两台电脑同时生成后再直接合并数据库文件。
- 后续优化：把用量账本迁移到远程 PostgreSQL 等中心数据库，本地 SQLite 只作断网缓冲，解决并发写入和 Git 二进制冲突。

### 3.7 本次完成：P0 首发候选产品

- 将 30 张样卡扩展为 45 张“项目管理核心卡”，版本升至 `1.0.0`。
- 复核原有 30 张，压缩 6 张过长答案；新增 15 张，补足质量、资源、沟通、执行、监控和收尾等主题。
- 发现并移除误标为 2023 版、实际为 2016 年第 2 版的旧大纲 PDF，改用清华大学出版社第 3 版页面和 2023 年审定大纲目录核对范围。
- 已生成 45 卡完整 APKG 和 10 卡免费试看 APKG。
- 新增质量报告、闲鱼标题与详情页、首发定价实验、交付说明、版本记录和勘误流程，位于 `content/product/system-integration-pm/`。
- 当前标记为“首发候选版”；正式收费前仍需持有正版第 3 版教程的人工逐页核对章节定位。

### 3.8 本次新增：卡片类型控制、长数量补齐与内容折叠

- 智能拆卡建议与生成设置合并为一个板块，默认使用混合模式。
- 支持选择一种或多种卡片类型；分析后会展示本次预计的类型数量分布。
- 服务端严格限制模型输出只能落在用户选择的类型集合内，避免模型自行混入其他类型。
- 实测单选问答只返回 `qa`；多选对比和步骤只返回 `compare/sequence`。
- 生成数量较大时改为分批调用，单次最多请求 24 张；后续批次携带已有问题用于去重，数量不足会自动补齐。
- SSE 完成消息同时返回计划数、实际数和是否补齐；若最终仍不足，页面明确提示实际数量。
- ASCII、Mermaid 和文字架构图不再生成“画出/复现/默写完整架构图”卡，而是拆成层职责、相邻层调用关系和关键数据流。
- 分析完成后学习材料正文默认折叠为短预览，图表或代码块不再整段铺开；可点击“展开全文编辑”。
- 新增默认折叠的“分析逻辑是什么？”说明，解释结构信号、AI 原子知识点抽取和三档数量计算。
- 使用不含私人内容的合成架构材料实测：计划 42 张、实际 42 张、精确重复 0、整图默写卡 0。
- 私人同步盘中的 DB-GPT 原笔记未由 Codex 重新发送给外部模型；如需原文回归，必须取得用户明确授权。

### 3.9 本次新增：SQLite 卡组库

- 新增 `data/decks.sqlite`，使用 `decks` 和 `cards` 两张表保存卡组及卡片顺序；该文件可能包含用户材料生成的私人卡片，因此已加入 `.gitignore`，禁止提交 GitHub。
- 新增 `/api/decks`，支持读取、保存、同名覆盖、批量迁移和删除卡组。
- 旧版 `ankicard.saved-decks.v1` LocalStorage 数据会在页面首次打开时批量迁移；只有服务端写入成功后才删除浏览器旧数据，失败时保留旧数据作为兜底。
- `content/decks/system-integration-pm-sample.json` 仍是内置产品卡组的可审查源文件；服务端启动后自动按版本号导入 SQLite。目前 `1.0.0` 的 45 张卡已入库。
- 内置卡组在页面标记版本号并禁止删除；用户生成的卡组可以保存、覆盖、删除和导出。
- 当前 SQLite 是单机存储，公司和家里电脑各有一份，不会通过 Git 自动同步；后续账号系统上线后迁移到 PostgreSQL 等中心数据库。

### 3.10 本次新增：2016—2017 真题离线导入试验

- 新增 `scripts/exam_pipeline/`：使用 Poppler 拆页、RapidOCR + ONNX Runtime 离线识别、规则遮罩广告、结构化选择题和案例题，并生成审核 HTML/JSON。整个试验没有调用 DeepSeek，不产生大模型 Token。
- 已目视确认三套卷的 PDF 物理页边界：2017 年 11 月为 232—256 页，2017 年 05 月为 257—282 页，2016 年 11 月为 283—305 页，共 74 页。
- 原 PDF 按 SHA-256 文档 ID 复制到 `data/uploads/c379119c009fe98a/source.pdf`；74 张原页图和遮罩图保存在 `data/exam-assets/c379119c009fe98a/`；以上文件及 `data/exam-content.sqlite` 均已加入 `.gitignore`。
- SQLite 新增文档、试卷、页面、遮罩和题目结构。三套卷均已形成 1—75 连续的上午选择题，共 225 题；下午案例题拆出 16、12、14 个小问，共 42 个小问，案例参考答案均已关联。
- 平均页面 OCR 置信度分别约为 0.9864、0.9890、0.9853。针对重复页眉、底部广告以及 PDF 第 251、277、299、305 页的大块二维码推广设置了非破坏遮罩；原图始终保留。页底遮罩起点已由 92.5% 下移到 95.5%，避免误伤紧邻广告的末行选项和答案；宽幅低置信度行会自动局部重识别。
- 三套卷 225 道选择题的题号、四个选项和答案现已通过自动完整性检查，结构异常为 0；这只代表字段齐全，OCR 字词和图表归属仍需人工抽查，不能直接等同于内容完全正确。
- 已修正题号解析误吞题干开头 `（）`、中文顿号/英文冒号选项拆分、页底印刷页码混入末项的问题；对“【签案】f”等损坏答案标记只作为字段边界，不再污染 D 选项，也不盲猜答案。2017-11 第 18 题答案 C、第 47 题字母 I，以及 2017-05 第 29 题漏失的 B 选项均已根据原页核对，通过独立人工修订层保存，重复拆题不会覆盖修订。
- SQLite 新增 `question_corrections` 和 `question_assets`：人工修订与 OCR 原始结果分离保存；题目级媒体记录原页、裁剪坐标和文件路径。图形检测会先剔除 OCR 文字框，再识别剩余的大块连线、图标和表格，只为真正的图表生成媒体，不再把普通文字解析整段截图。当前三套卷自动关联 33 个图形资产；2017-11 第 17 题的数据仓库图归入解析，第 29 题的组织结构图归入题干，2017-05 第 28 题的组织结构表归入解析。
- 静态审核入口仍保留在 `data/exam-assets/c379119c009fe98a/review/index.html`，详细检查结果为同目录 `validation.json`。平台顶部已新增“真题审核”工作台和 `/api/exams`：可选试卷、筛选异常/已修改/待审核题、编辑题干/A—D选项/答案/解析，并将误裁图片切换为裁剪图、来源整页或隐藏。修订写入 `question_corrections`，图片选择写入 `question_asset_overrides`，不覆盖 OCR 原始结果。
- 平台已实现确认门禁：保存题目或修改图片会把试卷状态重置为 `reviewing`；只有字段完整且用户点击“确认整卷”后，才能选择最多20题送入生成页，再由 DeepSeek 分析并拆卡。第48题已在平台中移除混入题干的图中文字并改用来源整页，第47题题干的数字 `1` 已修正为字母 `I`。通用 PDF 网页上传、页段配置和异步 OCR 队列仍待实现。
- PDF、页面图片和 SQLite 运行库不提交 Git；已核对的少量人工修订保存在 `scripts/exam_pipeline/corrections.json`，家里电脑取得同一份源 PDF 并重新运行流水线后，会自动恢复第18/29/47/48题修订和第48题“来源整页”设置。网页后续新增的其他修订仍只在本机 SQLite，未来需增加修订导入/导出或云端同步。

## 4. 当前本地配置状态

- 公司电脑的 DeepSeek 已配置并完成过最小连通测试。
- 公司电脑的飞书群机器人 Webhook 已被服务端识别。
- 企业微信群机器人尚未配置。
- `.env.local` 被 Git 忽略。回家后需要在家里电脑重新创建并填写自己的值，不能从 GitHub 获得。

变量名如下，真实值不要写入本文件：

```dotenv
DEEPSEEK_API_KEY=
FEISHU_WEBHOOK_URL=
WECOM_WEBHOOK_URL=
CUSTOM_LLM_PROVIDER_NAME=
CUSTOM_LLM_API_KEY=
CUSTOM_LLM_BASE_URL=
CUSTOM_LLM_MODELS=
ENABLE_DEMO_MODEL=false
```

如果只想测试页面流程，可临时设置：

```dotenv
ENABLE_DEMO_MODEL=true
```

验证结束后建议改回 `false`，再重启开发服务。

## 5. 2026-07-31 已完成验证

- TypeScript：`tsc -p tsconfig.json --noEmit` 通过。
- ESLint：本次修改的 5 个 TypeScript 文件检查通过，无错误或警告。
- 生产构建：`next build` 通过，`/api/analyze-content` 已包含在构建路由中。
- 浏览器端到端流程使用本地演示模型验证通过：
  1. 加载 RAG 示例材料。
  2. 分析识别 7 个知识点、4 个核心知识点。
  3. 给出精简 4 张、标准 8 张、全面 11 张建议。
  4. 按标准方案生成 8 张卡片。
  5. 修改第 1 张卡片的问题并保存。
  6. 保存到“我的卡组”。
  7. 成功导出包含 8 张卡片的 APKG。
- 还验证了自定义 50 张时会显示“可能出现重复”的质量警告。
- 修改材料后会显示“请重新分析”，生成按钮会禁用。
- DeepSeek 请求在受限测试沙箱无法访问网络时，会正确退回本地估算；这不代表用户电脑上的 DeepSeek Key 失效，应以页面“模型设置 → 测试”的结果为准。

## 6. 当前限制与已知问题

- “我的卡组”保存在当前运行服务所在电脑的 `data/decks.sqlite`，换电脑不会自动同步；浏览器清理缓存不再删除卡组，但删除 SQLite 文件会丢失本机用户卡组。
- 已有本地 SQLite 卡组库和模型用量库，但暂无用户账号、云端数据库、会员额度、支付和订单系统；`/api/decks` 目前只适合本地使用，公开部署前必须增加身份与权限隔离。
- 目前分析和生成只处理前 20,000 字符，尚未实现超长文档的分块、跨块去重与合并。
- 文件导入以可直接提取文本的格式为主，尚不支持 PDF/Word/PPT 的高质量版面解析。
- 尚未实现扫描件、图片或手写笔记 OCR。手写识别应放在基础文本拆卡质量稳定之后，优先级低于 PDF/Word 和长文档分块。
- AI 给出的知识点和建议数量是规划辅助，仍需人工检查事实准确性与卡片价值。
- 飞书和企业微信当前只能向群机器人即时推送，不能按用户单聊、定时推送或记录复习结果。
- 尚无完整自动化测试套件，目前关键流程主要靠类型检查、生产构建和浏览器端到端手测。
- 大数量生成通过重复发送材料实现分批补齐，可靠性更高但 Token 消耗也会增加；管理页会按每次模型调用记录批次号和逻辑目标数。
- 当前跨批去重包含精确问题去重和提示词约束，尚未实现基于向量相似度的语义重复检测。

## 7. 下一步待办（按建议优先级）

### P0：把软考样卡做成可出售的最小产品（首发候选已完成）

1. 已完成 45 卡首发候选包、10 卡试看包、商品文案、定价实验、交付说明、版本号和勘误流程。
2. 上架前剩余人工门槛：持正版第 3 版教程逐页核对 45 张卡的术语与章节定位。
3. 上架后按 7 天或 300 次曝光记录收藏、咨询、试看领取、成交和退款，只记录汇总数据。

### P1：继续增强卡片生成质量

1. 增加生成后的质量检查：语义重复检测、答案长度、模糊指代、问题泄露答案、来源缺失。
2. 允许用户在生成前查看、增删、合并 AI 识别出的知识点，再确认数量。
3. 对不同材料类型采用不同数量策略，例如概念教材、公式章节、流程规范、错题解析。
4. 增加批次生成与跨批次去重，为长教材和大题库做准备。
5. 保存分析结果与生成参数，便于重新生成和对比不同模型。

### P2：增强输入格式和大文件能力

1. 优先支持 PDF、DOCX 和 PPTX 的正文、标题层级和表格提取。
2. 实现大文件分块、进度显示、断点恢复、章节级生成和合并去重。
3. 图片/扫描件 OCR 放在其后；印刷体优先，手写 OCR 最后评估，因为准确率、成本和版面还原难度更高。
4. 上传能力要增加文件大小限制、类型校验、超时处理和隐私说明。
5. 已抽查一份 2016—2022 中项真题解析 PDF：约 109MB、305 页，除目录页外 304 页均无可提取文字，实际是整页扫描图；包含重复页眉页脚、二维码广告、斜向水印、表格、网络图和案例题。此类文件不能直接交给大模型，计划采用“流式上传 → 按年份/卷别切分 → 页面渲染 → OCR/版面分析 → 广告块标记 → 题目结构化 → 人工复核 → 生成卡片”的异步流水线。
6. PDF 原文件及页面图片不存 SQLite、不提交 Git；对象存储或本地私有目录保存文件，SQLite 只记录文档、分卷、页码、题目、图片坐标、OCR 结果、处理状态和来源关系。
7. 后续增加“导入预处理编辑器”：OCR 前展示页面缩略图，允许用户框选广告、二维码、水印、页眉页脚等无关区域，并选择仅当前页、指定页段、同版式页面或整份文档应用；遮罩必须可撤销、可预览且不修改原始文件。遮罩坐标和适用页码存 SQLite，后台只对保留区域执行 OCR，从源头减少噪声与后续大模型 Token。首版先做矩形框选、复制到连续页和恢复原图，自动识别同版式及不规则画笔擦除以后再评估。
8. 已完成三套 2016—2017 真题的离线流水线验证和 SQLite 入库，225 道选择题自动结构异常已清零；选择题审核编辑器和确认门禁已经接入平台。下一步不是继续扩大年份，而是人工抽查 OCR 内容、确认 42 个案例小问的图表归属，再实现网页上传、页段配置和异步任务。
9. 平台集成时必须设置“人工审核确认”门禁：OCR 和结构化完成后先进入可编辑审核页，允许修改题干、选项、答案、解析和图表归属，并支持逐题审核状态、异常筛选、保存草稿和整卷确认。只有整卷状态变为 `confirmed` 后才能调用 DeepSeek 生成卡片；任何源题修改都应使旧确认失效并要求重新确认。生成记录需保存所用题目版本，避免审核后改题导致卡片来源不可追溯。

### P3：从本地工具升级会员 SaaS

1. 增加用户登录、数据库和服务端卡组。
2. 将本地 SQLite 卡组迁移到云端，实现公司/家庭电脑同步。
3. 增加模型调用额度、套餐、订单和会员权限。
4. 统计导入、分析、生成、编辑、导出和复习转化数据，用于判断真正有价值的付费功能。

### P4：推送与复习闭环

1. 保留现有群 Webhook 作为最简单方案。
2. 接入飞书应用机器人，实现用户授权、按用户单聊和身份映射。
3. 增加定时任务、间隔重复计划、答题按钮和学习记录。
4. 之后再评估企业微信应用、微信公众号或微信 Bot；涉及个人微信自动化时要先确认平台合规与账号风险。

### P5：AnkiDroid 界面二开（已决定延期）

1. 后续单独 Fork AnkiDroid，只重做卡组首页、复习页、添加/编辑页等界面，保留复习算法、数据库、同步和其他业务逻辑。
2. 第一阶段目标是换品牌、主题和三个核心页面，并接入 AnkiCard AI 生成入口；验证 APK 稳定后再决定是否覆盖全部页面。
3. 需要单独维护 Android/Kotlin 工程、上游合并和 GPL/AGPL 合规，不与当前 Next.js 项目直接混在同一代码目录。
4. 当前优先级低于软考首发验证、PDF 真题结构化和卡片质量检查器。

## 8. 回家电脑继续开发

如果仓库已经克隆：

```powershell
cd D:\project\ankicard
git pull origin main
pnpm install
Copy-Item .env.example .env.local
```

然后编辑 `.env.local`，至少填写：

```dotenv
DEEPSEEK_API_KEY=你自己的Key
```

如需继续测试飞书，再填写家里可用的 `FEISHU_WEBHOOK_URL`。启动：

```powershell
pnpm dev
```

打开 <http://localhost:5000>。推荐先进入“模型设置”测试 DeepSeek，再用“示例 → 分析内容并推荐数量 → 生成卡片”验证环境。

注意：如果家里的项目路径不是 `D:\project\ankicard`，按实际路径进入即可。

## 9. 和 Codex 跨电脑续接

聊天记录是否能看到取决于是否使用同一个 Codex/OpenAI 账号以及该任务是否同步；代码进度以 GitHub 和本文件为准，不依赖聊天记录。

在家里开启新的 Codex 任务时粘贴：

```text
这是 AnkiCard AI 项目。请先阅读 AGENTS.md、README.md、HANDOFF.md、package.json 和最近的 git log，再检查 git status，然后从 HANDOFF.md 的 P0 待办继续。不要读取、展示或提交 .env.local，也不要使用 npm/yarn。
```

## 10. 每次下班前的交接流程

1. 更新本文件的已完成、验证、限制和下一步待办。
2. 用 `git diff` 检查变更，确认没有 Key、Webhook、个人信息和无关大文件。
3. 运行类型检查、Lint 和生产构建。
4. 提交并推送到 `origin/main`。
5. 在另一台电脑先 `git pull origin main`，再开始修改。
